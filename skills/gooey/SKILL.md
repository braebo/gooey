---
name: gooey
description: Before building or changing a gooey panel — read the manual, use the API, write no glue.
---

# Gooey

Read `node_modules/gooey/README.md` whole, then the options type of each input you add (`node_modules/gooey/dist/inputs/Input*.d.ts`). What a panel needs is an option already; glue means the read was skipped.
