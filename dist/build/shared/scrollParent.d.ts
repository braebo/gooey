/**
 * Checks if a node is scrollable.
 */
export declare const isScrollable: (node: Element) => boolean;
/**
 * Resolves the nearest scroll parent of a node.
 */
export declare const getScrollParent: (node: Element) => Element;
